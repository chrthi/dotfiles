
# This file is sourced by bash when run as a login shell.
# Since only the first of .bash_profile, .bash_login and .profile is sourced,
# we need to source .profile manually.
source ~/.profile
source ~/.bashrc
